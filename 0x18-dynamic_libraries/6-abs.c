#include "main.h"

/**
 * _abs - function that computes the absolute value of an int
 * @n: integer whos value will be computed
 *
 * Return: absolute value if an int
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	return (-n);
}
