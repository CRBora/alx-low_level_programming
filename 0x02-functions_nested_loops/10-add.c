#include "main.h"

/**
 * add - function to add 2 integers and print the result
 * @l: first integer
 * @k: second int
 * Return: sum of int l and int k to stdout
 */
int add(int l, int k)
{
	return (l + k);
}
